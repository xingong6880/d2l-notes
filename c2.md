

### C2: 索引和切片分割（Indexing and Slicing）

```python
# 假设 X 是上一节中定义的 3x4 张量
# 索引最后一行
X[-1]

# 切片获取第 1 行到第 2 行（不包括第 3 行）
X[1:3]

# 将第 1 行第 2 列的元素赋值为 17
X[1, 2] = 17

# 将第 0 行和第 1 行（:2）的所有列（:）赋值为 12
X[:2, :] = 12
```

### C3：操作（operation）

```python
# 假设 x 是 0 到 11 的一维张量
# 对 x 中的每个元素计算 e 的指数
torch.exp(x)

# 两个张量之间的按元素运算：
# addition (+), subtraction (-), multiplication (*), division (/), exponentiation (**)

# 假设 X 和 Y 是两个形状相同的张量
# 沿维度 0 (行) 连接两个张量
torch.cat((X, Y), dim=0)

# 沿维度 1 (列) 连接两个张量
torch.cat((X, Y), dim=1)

# 按元素比较 X 和 Y 是否相等，返回一个布尔张量
X == Y

# 计算 X 中所有元素的总和
X.sum()
```

### C4：广播机制 (broadcasting mechanism)

在张量形状不一致时，PyTorch 能自动“扩展”（不实际复制内存）小张量，使其与大张量形状兼容，从而执行逐元素运算。

```python
# 示例
a = torch.arange(3).reshape((3, 1))  # a 的形状: (3, 1)
b = torch.arange(2).reshape((1, 2))  # b 的形状: (1, 2)
# a → (3,2): [[0,0], [1,1], [2,2]]
# b → (3,2): [[0,1], [0,1], [0,1]]
a + b # 结果是 tensor([[0,1], [1,2], [2,3]])
```

### C5：Saving memory (节省内存)

张量内存与就地操作

```python
# 假设 X 和 Y 形状相同
# 普通操作：Y = Y + X 会创建新内存
Y = Y + X # id(Y) 会改变

# 就地操作：Y[:] = Y + X 在原内存上修改
Y[:] = Y + X # id(Y) 不变

# 示例：
# 创建一个与 Y 形状相同的全 0 张量
Z = torch.zeros_like(Y)
# 使用就地操作将 X + Y 的结果存入 Z
Z[:] = X + Y
```

### C6：张量与 Python 对象的互相转换

```python
# 假设 X 是一个 PyTorch 张量
# 张量转 NumPy 数组
A = X.numpy()

# NumPy 数组转张量
B = torch.from_numpy(A)

# 查看类型
type(A), type(B)

# --- 张量转标量（size-1 张量）---
a = torch.tensor([3.5])

# 使用 .item() 获取 Python 标量 (float)
a.item()

# 使用 float() 转换
float(a)

# 使用 int() 转换 (会截断)
int(a)
```

## 2. Data preprocessing (数据预处理)

### C1: 阅读数据集

```python
import os
import pandas as pd

# 1. 创建目录 (如果 'data' 目录不存在)
os.makedirs(os.path.join('..', 'data'), exist_ok=True)

# 2. 指定 CSV 文件路径
data_file = os.path.join('..', 'data', 'house_tiny.csv')

# 3. 将数据内容写入文件
with open(data_file, 'w') as f:
    f.write('''NumRooms,RoofType,Price
NA,NA,127500
2,NA,106000
4,Slate,178100
NA,NA,140000''')

# 4. 使用 pandas 读取 CSV 数据
data = pd.read_csv(data_file)

# 5. 打印数据
print(data)
```

### C2：Data Preparation (数据准备)

```python
# 假设 data 是上一节中读取的 DataFrame
# 1. 分割特征 (inputs) 和标签 (outputs)
inputs, outputs = data.iloc[:, 0:2], data.iloc[:, 2]

# 2. 特征编码及缺失值处理
# (注意：df 在这里是泛指，实际应使用 inputs)

# (I) 处理类别变量：将 object 类型转为独热编码，NaN 也视为一个类别
# 假设 inputs 包含需要转换的列
inputs_processed = pd.get_dummies(inputs, dummy_na=True)

# (II) 处理数值缺失值：用该列的平均值填充 NaN
# （get_dummies 后的 inputs_processed 可能仍有数值列的 NA，
#  或者在 get_dummies 前单独处理）
# 示例：假设 'NumRooms' 是 inputs 中的数值列
# inputs['NumRooms'] = inputs['NumRooms'].fillna(inputs['NumRooms'].mean())
# 更通用的方法 (对所有数值列):
inputs_numeric = inputs.select_dtypes(include='number')
inputs_numeric = inputs_numeric.fillna(inputs_numeric.mean())

print(inputs_numeric) # 查看填充结果
print(inputs_processed) # 查看独热编码结果


**数据预处理常用函数：**
`os.makedirs` / `os.path.join` / `open`
`pd.read_csv`
`DataFrame.iloc`
`pd.get_dummies` (作用于 object 类别)
`DataFrame.fillna(DataFrame.mean())` (作用于数值列)
```
### C3: Conversion to the Tensor Format (转换为张量格式)

```python
import torch

# 假设 inputs_processed 和 outputs 是处理好的 pandas 数据
# 1. 将 DataFrame 转换为 NumPy 数组，再转换为张量
# (注意：这里使用上一节的变量名 inputs_processed 和 outputs)
X = torch.tensor(inputs_processed.to_numpy(dtype=float))
y = torch.tensor(outputs.to_numpy(dtype=float))

# 2. 查看转换后的张量
print(X)
print(y)
```

## 3. Linear Algebra (线性代数)

### C1：scalar arithmetic (标量运算)

(emm, ez)

### C2：Vectors (向量)

(文本内容)

### C3：Matrices (矩阵)

```python
# 创建一个 0-5 的张量并重塑为 3x2
A = torch.arange(6).reshape(3, 2)

# 获取矩阵 A 的转置
A.T

# 创建一个对称矩阵
A_sym = torch.tensor([[1, 2, 3], [2, 0, 4], [3, 4, 5]])

# 检查矩阵是否对称
A_sym == A_sym.T
```

### C4: Tensors (张量)

```python
# 创建一个 0-23 的张量并重塑为 2x3x4 (三阶张量)
torch.arange(24).reshape(2, 3, 4)
```

### C5：Basic Properties of Tensor Arithmetic

```python
A = torch.arange(6, dtype=torch.float32).reshape(2, 3)

# 分配新内存，将 A 的副本赋给 B
B = A.clone()

# Hadamard product (哈达玛积)：按元素乘法
A * B
```

### C6: Reduction (归约)

```python
x = torch.arange(12, dtype=torch.float32)
# 计算 x 中所有元素的和
x.sum()

A = torch.arange(6, dtype=torch.float32).reshape(2, 3)
# 查看 A 的形状
A.shape

# 沿维度 0 (行) 求和，输出形状 (3,)
A.sum(axis=0)
A.sum(axis=0).shape

# 沿维度 1 (列) 求和，输出形状 (2,)
A.sum(axis=1)
A.sum(axis=1).shape

# 对所有元素求和，输出标量 (形状为 ())
A.sum()
A.sum().shape

# 检查按所有轴求和是否等于 .sum()
A.sum(axis=[0, 1]) == A.sum()

# 求所有元素的平均值
A.mean()
A.sum() / A.numel() # 效果相同

# 沿维度 0 求平均值
A.mean(axis=0)
A.sum(axis=0) / A.shape[0] # 效果相同
```

### C7：Non-Reduction Sum (无归约求和)

```python
# 假设 A 是 2x3 张量
# 沿维度 1 (列) 求和，并保持维度 (keepdims=True)
sum_A = A.sum(axis=1, keepdims=True)
# sum_A 的形状会是 (2, 1) 而不是 (2,)

# 查看结果和形状
print(sum_A)
print(sum_A.shape)

# 利用广播机制，A (2,3) 除以 sum_A (2,1)
# 得到每行归一化的矩阵 (每行之和为 1)
A / sum_A

# 沿维度 0 (行) 计算累积和
A.cumsum(axis=0)
```

### C8：Dot Products (点积)

```python
x = torch.arange(3, dtype=torch.float32) # tensor([0., 1., 2.])
y = torch.ones(3, dtype = torch.float32) # tensor([1., 1., 1.])

# 计算 x 和 y 的点积
torch.dot(x, y) # 结果为 tensor(3.)

# 也可以通过按元素乘法后求和实现
torch.sum(x * y) # 结果为 tensor(3.)
```

### C9：Matrix–Vector Products (矩阵–向量乘法)

```python
# A 是 2x3 矩阵, x 是长度为 3 的向量
A = torch.arange(6, dtype=torch.float32).reshape(2, 3)
x = torch.arange(3, dtype=torch.float32)

# 矩阵-向量乘法
torch.mv(A, x) # 结果 tensor([ 5., 14.])

# 也可以使用 @ 运算符
A @ x # 结果 tensor([ 5., 14.])
```

### C10：Matrix–Matrix Multiplication (矩阵乘法)

```python
# A 是 2x3 矩阵
A = torch.arange(6, dtype=torch.float32).reshape(2, 3)
# B 是 3x4 矩阵
B = torch.ones(3, 4)

# 矩阵-矩阵乘法
torch.mm(A, B)

# 也可以使用 @ 运算符
A @ B
```

### C11: Norms (范数)

```python
u = torch.tensor([3.0, -4.0])

# 计算 L2 范数 (欧几里得长度)
torch.norm(u) # 结果 tensor(5.)

# 计算 L1 范数 (绝对值之和)
torch.abs(u).sum() # 结果 tensor(7.)

# 计算 4x9 矩阵的 Frobenius 范数
torch.norm(torch.ones((4, 9))) # 结果 tensor(6.)

# torch.linalg.norm() 是更现代的接口
# 创建一个 2x3x4 的张量
X = torch.arange(24., dtype=torch.float32).reshape(2, 3, 4)

# 计算该高维张量的范数 (所有元素平方和的平方根)
torch.linalg.norm(X)
```

## 4. Calculus (微积分)

```python
%matplotlib inline
import numpy as np
from matplotlib_inline import backend_inline
from d2l import torch as d2l
```

### C1: Derivatives and Differentiation (导数与微分)

(ez)

### C2：Visualization Utilities (可视化工具集)

```python
def use_svg_display(): #@save
    """在 Jupyter 中使用 svg 格式显示绘图。"""
    backend_inline.set_matplotlib_formats('svg')

def set_figsize(figsize=(3.5, 2.5)): #@save
    """设置 matplotlib 的图像大小。"""
    use_svg_display()
    d2l.plt.rcParams['figure.figsize'] = figsize

#@save
def set_axes(axes, xlabel, ylabel, xlim, ylim, xscale, yscale, legend):
    """设置 matplotlib 的坐标轴。"""
    axes.set_xlabel(xlabel)
    axes.set_ylabel(ylabel)
    axes.set_xscale(xscale)
    axes.set_yscale(yscale)
    axes.set_xlim(xlim)
    axes.set_ylim(ylim)
    if legend:
        axes.legend(legend)
    axes.grid()

#@save
def plot(X, Y=None, xlabel=None, ylabel=None, legend=[], xlim=None,
         ylim=None, xscale='linear', yscale='linear',
         fmts=('-', 'm--', 'g-.', 'r:'), figsize=(3.5, 2.5), axes=None):
    """绘制数据点。"""
    
    def has_one_axis(X): # 辅助函数，检查 X 是否只有一个轴
        return (hasattr(X, "ndim") and X.ndim == 1 or isinstance(X, list)
                and not hasattr(X[0], "__len__"))

    if has_one_axis(X): X = [X]
    if Y is None:
        X, Y = [[]] * len(X), X
    elif has_one_axis(Y):
        Y = [Y]
    if len(X) != len(Y):
        X = X * len(Y)
    
    set_figsize(figsize)
    if axes is None:
        axes = d2l.plt.gca() # 获取当前坐标轴
    axes.cla() # 清除当前坐标轴
    
    for x, y, fmt in zip(X, Y, fmts):
        if len(x):
            axes.plot(x, y, fmt)
        else:
            axes.plot(y, fmt)
    
    set_axes(axes, xlabel, ylabel, xlim, ylim, xscale, yscale, legend)
```

### C3: Partial Derivatives and Gradients (偏导与梯度)

(需要看视频！这里不懂、、、)

### C4：Chain Rule (求导的链式法则)

(文本内容)

## 5. Automatic Differentiation (自动微分)

### C1：simple function (简单函数)

```python
import torch

# 1. 创建一个张量 x
x = torch.arange(4.0)

# 2. 告诉 PyTorch 我们需要计算关于 x 的梯度
x.requires_grad_(True)

# 3. 在调用 backward 之前，梯度是 None
print(x.grad) # 输出: None

# 4. 定义一个函数 y = 2 * x·x
y = 2 * torch.dot(x, x)
print(y) # 输出: tensor(28., grad_fn=<MulBackward0>)

# 5. 执行反向传播，计算 dy/dx
y.backward()

# 6. 打印 x 的梯度 (dy/dx = 4x)
print(x.grad) # 输出: tensor([ 0., 4., 8., 12.])

# 7. 验证梯度是否正确
print(x.grad == 4 * x) # 输出: tensor([True, True, True, True])

# 8. 清零梯度，为下一次计算做准备
x.grad.zero_()

# 9. 计算 y = sum(x)
y = x.sum()

# 10. 执行反向传播
y.backward()

# 11. 打印 x 的梯度 (dy/dx_i = 1)
print(x.grad) # 输出: tensor([1., 1., 1., 1.])
```

### C2: Backward for Non-Scalar Variables (非标量变量的反向传播)

在 PyTorch 中，只有标量（即 shape 为 () 的张量）可以直接调用 `.backward()`。
如果你想对非标量张量（例如向量或矩阵）进行反向传播，必须先将其“归约”为标量，或者显式传入一个与其形状相同的 `gradient` 参数。

```python
x = torch.arange(4.0)
x.requires_grad_(True)

# y 是一个向量 (非标量)
y = x * x
print(y) # 输出: tensor([0., 1., 4., 9.], grad_fn=<MulBackward0>)

# 不能直接调用 y.backward()
# 我们传入一个与 y 相同形状的梯度张量 (通常是全 1)
# 这相当于计算 L = sum(y * gradient)，然后求 dL/dx
y.backward(torch.ones_like(x)) # 相当于对 sum(y) 求导

# 打印梯度 (dy_i/dx_i = 2x_i)
print(x.grad) # 输出: tensor([0., 2., 4., 6.])
```

### C3：Detaching Computation (分离计算)

有时我们希望中断梯度传播，或者从当前计算图中分离出一个张量，这时就需要使用 `.detach()`。

```python
x = torch.arange(4.0, requires_grad=True)
# x.grad.zero_() # 这行是多余的，因为 x 刚创建，梯度本就是 None 或 0

# 计算 y = x*x
y = x * x

# 1. 分离 u
# u 是 y 的一个新张量，它与 y 共享数据，但不参与梯度计算
u = y.detach()

# 2. z 的计算依赖于 u 和 x
# z = u * x = (x*x) * x = x³
# 但是因为 u 被分离了，梯度只会通过 x 传播
z = u * x

# 3. 对 z 求和并反向传播
z.sum().backward()

# 4. 计算 dz/dx
# dz/dx = d(u * x)/dx = u * (dx/dx) + (du/dx) * x
# 因为 du/dx = 0 (u被分离了)
# 所以 dz/dx = u = x*x
print(x.grad) # 输出: tensor([0., 1., 4., 9.])

# 5. 清空梯度
x.grad.zero_()

# 6. 对 y 本身进行反向传播
# y 的计算图 (y = x*x) 仍然存在
y.sum().backward()

# 7. 计算 dy/dx = 2x
print(x.grad == 2 * x) # 输出: tensor([True, True, True, True])
```

### C4：Gradients and Python Control Flow (梯度与 Python 控制流)

PyTorch 的自动微分支持包含 Python 控制流（如 if、while）的计算图。

```python
def f(a):
    """一个包含控制流的函数"""
    b = a * 2
    # while 循环，次数取决于 a 的值
    while b.norm() < 1000:
        b = b * 2
    # if 条件判断
    if b.sum() > 0:
        c = b
    else:
        c = 100 * b
    return c

# 1. 创建一个需要梯度的随机张量 a
a = torch.randn(size=(2,), requires_grad=True)

# 2. 执行前向传播
d = f(a)

# 3. 执行反向传播
d.backward()

# 4. 打印梯度
print(a.grad)

# 5. 验证梯度
# 因为 f(a) 最终是一个线性函数 f(a) = k*a 的形式（k 是常数）
# 所以 d/da = k = f(a)/a = d/a
print(a.grad == d / a)
```

### C5: Conclusion (总结)

(文本内容)

## 6. Probability and Statistics (概率与统计)

(文本内容)

### C1：simple example：Tossing coins (抛硬币)

```python
import random
import torch
from torch.distributions.multinomial import Multinomial
from d2l import torch as d2l

# 1. 模拟抛 100 次硬币
num_tosses = 100
# 使用列表推导式计算正面朝上的次数
heads = sum([random.random() > 0.5 for _ in range(num_tosses)])
tails = num_tosses - heads
print("heads, tails: ", [heads, tails])

# 2. 使用 PyTorch 的多项分布模拟器
# 定义公平硬币的概率 [0.5, 0.5]
fair_probs = torch.tensor([0.5, 0.5])
# 模拟 100 次试验
Multinomial(100, fair_probs).sample() # 例如 tensor([50., 50.])

# 3. 模拟 10000 次，并观察概率估计的收敛
# 模拟 10000 次，每次试验 1 次
counts = Multinomial(1, fair_probs).sample((10000,))
# 计算累积次数
cum_counts = counts.cumsum(dim=0)
# 计算累积概率估计
estimates = cum_counts / cum_counts.sum(dim=1, keepdims=True)
estimates_np = estimates.numpy() # 转换为 NumPy 以便绘图

# 4. 绘图
d2l.set_figsize((4.5, 3.5))
d2l.plt.plot(estimates_np[:, 0], label=("P(coin=heads)"))
d2l.plt.plot(estimates_np[:, 1], label=("P(coin=tails)"))
d2l.plt.axhline(y=0.5, color='black', linestyle='dashed') # 绘制 0.5 参考线
d2l.plt.gca().set_xlabel('Samples')
d2l.plt.gca().set_ylabel('Estimated probability')
d2l.plt.legend();
```

### C2：A More Formal Treatment (更正式的处理)

(文本内容)

### C3：Random Variables (随机变量)

(文本内容)

### C4：Multiple Random Variables (多个随机变量)

(文本内容)

### C5：example (贝叶斯示例)

(文本内容)

### C6: Expectations (期望)

(文本内容)

## 7. Documentation (查阅文档)

```python
import torch

# 1. 查看模块中包含的所有函数和类
print(dir(torch.distributions))

# 2. 查看特定函数的帮助文档
help(torch.ones)
```
